apiVersion: v2
name: exflow
description: An exFlow Helm chart for Kubernetes
type: application
version: 1.4.16
appVersion: "v1.0.0-beta9"

# add postgresdb
dependencies:
  - name: postgresql
    version: 16.5.0
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
    tags:
      - database
